# build output
dist/
# generated types
.astro/
# local wrangler/miniflare state (KV cache, dev sessions) — machine-local only
.wrangler/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Claude Code machine-local settings (never commit)
.claude/settings.local.json

# Personal reference material (CV etc.) — never commit to the public repo
reference_files/

# NOTE: public/resume.pdf is now a MANAGED, served asset — uploaded and committed
# from the Studio Resume page (functions/api/admin/resume.js) and served at
# /resume.pdf. It is intentionally tracked (no longer ignored).

# Local env (Turnstile site key etc.)
.env
.dev.vars
